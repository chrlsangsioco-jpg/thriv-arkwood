import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Images are served via plain <img> from Supabase Storage (public URLs),
  // so no remote-image domain config is needed.
}

export default nextConfig
